Solutions to Leetcode Crack SQL Interview in 50 Qs
1
  a. select product_id
    from products
    where low_fats = 'y'  and recyclable = 'y';

  b. select name 
    from customer
    where referee_id != 2 or referee_id is null;

  c. select name,population,area
    from world
    where area>=3000000 or population>=25000000;

  d. select distinct(author_id) as id
    from views
    where author_id = viewer_id
    order by author_id asc;

  e. select tweet_id
    from tweets
    where length(content)>15;


2

  a. select EmployeeUNI.unique_id , Employees.name
    from Employees
    left join EmployeeUni
    on Employees.id = EmployeeUni.id;

  b.select Product.product_name, Sales.year,Sales.price
    from Sales,Product
    where sales.product_id=product.product_id;




7
  a.SELECT user_id, CONCAT(UPPER(SUBSTRING(name, 1, 1)), LOWER(SUBSTRING(name, 2))) AS name
    FROM Users
    ORDER BY user_id;

  b.SELECT * FROM PATIENTS WHERE
    CONDITIONS LIKE '% DIAB1%' OR CONDITIONS LIKE 'DIAB1%';

  c.DELETE p1
    FROM Person p1, Person p2
    WHERE p1.email = p2.email AND p1.id > p2.id;

  d.select max(salary) as SecondHighestSalary
    from employee 
    where salary != (select max(salary)
    from employee);

  e.SELECT
    sell_date,
    COUNT(DISTINCT product) AS num_sold,
    GROUP_CONCAT(distinct product ORDER BY product) AS products
    FROM activities
    GROUP BY sell_date
    ORDER BY sell_date;
